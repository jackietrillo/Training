Update today.js so neither argument can be an empty string.
Update today.js so that message can also be a number.
Update today.js so that the setText() function validates that the destination element was found.
